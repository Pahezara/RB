# ASSETS.md — asset register
Feeds `scripts/placeholders.mjs`. A row is satisfied when its status starts with
`HAVE` or `N/A`. Anything else blocks a production build.

`N/A` means the thing was deliberately dropped by a recorded decision, not forgotten.
Each one names the decision so it cannot quietly turn back into an oversight.

Every row below was checked against the client's own supplied documents on
2026-08-31.

| Asset | Status | Needed for | Source / how to obtain |
|---|---|---|---|
| `brand/rb-lockup.png` | **HAVE** | Palette, mark, favicons, OG | Supplied by SAJ 2026-08-31 as `RB Logo/RB_Logo transparent.png`. 2000x2000 PNG with real alpha. Copied into `brand/` and committed, because the build reads it; the rest of the supplied pack is gitignored. |
| Monogram-only file | **N/A** | — | The pack's `RB_Logo_Without name.png` measures fully opaque on every pixel with a white ground baked in. The monogram is cut out of the transparent lockup instead, so no threshold is guessed. Noted in `scripts/prepare-brand.mjs`. |
| `brand/PALETTE.md` | **HAVE** | `tokens.css`, `check-contrast.mjs` | Regenerated by `scripts/extract-brand.mjs` from the lockup. Navy `#163d64`, blue `#126797`, orange `#f47e1f`, gold `#fbc903` as cluster centroids; tokens sample the gradient endpoints. |
| Mark, light mark, favicon set, manifest | **HAVE** | Header, footer, OG, icons | `scripts/prepare-brand.mjs`. The light variant remaps only the blue half and is measured: as drawn the mark reaches 1.44:1 on the dark band, lifted it reaches 5.97:1, and the script fails below 3:1. |
| Rasterised lockup in the page chrome | **N/A** | — | Decision D8. The supplied lockup sets the firm's name on one line at roughly 24:1; at header size its letters would be three pixels tall. Header, footer and OG carry the monogram plus the name as real text. |
| Hero photograph | **HAVE** | Hero, and real depth behind the glass header | Unsplash, photo `1742277712272-aecf17e3accb` by Zoshua Colah, free under the Unsplash License. Source kept at `brand/hero-source.jpg`. `scripts/make-hero.mjs` crops it to the tower line to exclude third-party billboards, bakes the navy scrim, and fails the build if the type region drops below 4.5:1. |
| Per-page OG images | **HAVE** | Every page | `scripts/make-og.mjs`, 14 images, with a render probe that fails if the text does not draw. |
| Business profile content | **HAVE** | Home, about, services, JSON-LD | `RB Logo/RBPL Business Profile.pdf`, supplied 2026-08-31. Transcribed into `src/data/site.ts` with `[PROFILE]` provenance on every entry. |
| Secretarial fee structure | **HAVE** | `/packages/`, the estimator | `RB Logo/Secretarial fee Structure.xlsx`, supplied 2026-08-31. Transcribed into `src/data/site.ts` with `[FEES]` provenance. Not one figure is rounded. |
| Extras pricing | **HAVE** | The estimator, `/packages/` | Supplied by SAJ 2026-08-31: website from LKR 65,000; cloud POS from LKR 4,500 a month; offline POS from LKR 45,000 once. All three are labelled "from" everywhere they appear. |
| Enquiry actions | **HAVE** | Contact page | Direct actions: the phone, and a prefilled `mailto:` link. There is no form. A `mailto:` form POST made browsers warn that the submission was insecure and failed outright on most phones. A real form needs an HTTPS endpoint; see DECISIONS D10. |
| Opening hours | **N/A** | — | Decision D4. Not in the business profile and not supplied, so there is no `openingHoursSpecification` in the JSON-LD. Ask the client and this becomes a row again. |
| WhatsApp number | **N/A** | — | Decision D4. Not in the business profile and not supplied. The one published number is used for the phone action only; assuming it also answers WhatsApp is a guess the site will not make. |
| Postal code | **N/A** | — | Decision D4. Not given in any source. The address is published as the profile states it, and the JSON-LD carries no `postalCode` field rather than a plausible guess. |
| Registration / licence numbers | **N/A** | — | Decision D4. Not supplied. Worth asking for: for an accounting and secretarial firm it is the strongest remaining trust signal, and the four directors' professional bodies are already named in its place. |
| Testimonials / reviews | **N/A** | — | Decision D5. No proof section ships without real attributed reviews. |
| Office / team photos | **N/A** | — | Decision D5. No stock photo may stand in for a real office, and the board section is set as type rather than as empty photo cards. |
| Map embed / coordinates | **N/A** | — | No map embedded. The address is given as text and in JSON-LD. Revisit if a map is wanted. |
