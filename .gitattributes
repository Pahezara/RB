# Normalise to LF in the repository. The build runs on Linux; a checkout there
# should not differ from the working copy on Windows.
* text=auto eol=lf

# Binary assets, never touched.
*.png   binary
*.jpg   binary
*.jpeg  binary
*.webp  binary
*.avif  binary
*.woff2 binary
*.ico   binary
